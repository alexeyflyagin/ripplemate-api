POSTGRES_USER=ripplemate
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=ripplemate
POSTGRES_TEST_DB=ripplemate_test
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
SECRET_KEY=generate_with_python_-c_"import_secrets;_print(secrets.token_hex(32))"
CORS_ORIGINS=http://localhost:5173

# Base URL of the frontend, used to build reset/verify links in emails
FRONTEND_URL=http://localhost:5173
# Token lifetimes (minutes). Password reset short, email verify longer.
PASSWORD_RESET_TOKEN_TTL_MINUTES=30
EMAIL_VERIFY_TOKEN_TTL_MINUTES=1440
RESEND_API_KEY=
